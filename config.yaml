model: qwen2.5-coder:14b
# ollama_host: http://localhost:11434  # set when Ollama runs on a Windows host / remote box (e.g. from WSL2)
severity_threshold: info
min_confidence: 0.7
max_comments_per_file: 20
max_comments_per_pr: 10
ignore_paths:
  - .git/**
  - .venv/**
  - .uv-cache/**
  - .pytest_cache/**
  - .ruff_cache/**
  - __pycache__/**
